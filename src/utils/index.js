'use strict';

class Utils {
    static global = require('./global');
    static print = require('./log');
    static csv = require('./csv');
    static socket = require('./socket');
    static time = require('./time');
}
module.exports = Utils;
